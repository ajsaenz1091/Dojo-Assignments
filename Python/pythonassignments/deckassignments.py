class cards(object):
	def__init__(self,suit,value):
		self.suit=suit
		self.value=value
class Deck(object):
	def__init__(self):
		self.card.list+[]	
	def addcard(self,card):
		self.cardlist.append(card)
		return self
	def deal(self,cardlist):
		print self.cardlist[0]
		return self

for i in range(0,53):
	suis=''
	value= 0
	for s < len(suits):
		suit=suits[s]
	




for loop through suits
	for loop through values
		crreate a card
		push the deck list